// echo-loop 的 world：实现 dsh-loop 的 agent-loop 缝（loop 本身是 WASM 插件）。
package dsh:echo-loop;

use dsh:dsh/session;
use dsh:dsh/tools;
use dsh:dsh/llm;
use dsh:dsh/agent-loop;

world dsh-loop {
  export agent-loop;
  import session;
  import tools;
  import llm;
}
