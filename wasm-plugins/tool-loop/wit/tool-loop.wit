// tool-loop 的 world：实现 dsh-loop 的 agent-loop 缝，并在 run_turn 内调用 tools 缝。
package dsh:tool-loop;

use dsh:dsh/session;
use dsh:dsh/tools;
use dsh:dsh/llm;
use dsh:dsh/agent-loop;

world dsh-loop {
  export agent-loop;
  import session;
  import tools;
  import llm;
}
