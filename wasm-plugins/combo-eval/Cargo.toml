[package]
name = "combo-eval"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
# 与宿主 native 面**同源**：dsh-eval 编译进 wasm —— 结果一致性测试锚定的是 WASM
# 执行路径（ABI / JSON 编组 / 数值 / 错误传播）本身，而非另一套求值语义。
dsh-eval = { path = "../../crates/dsh-eval" }

[workspace]
