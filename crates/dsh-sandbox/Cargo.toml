[package]
name = "dsh-sandbox"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "M5 sandbox policy seam: modes, wider-mode ladder, escalation validation, writable roots, sandbox/mode session event, fail-closed policy resolution (port of the dsh sandbox capability seam)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dsh-session = { path = "../dsh-session" }
