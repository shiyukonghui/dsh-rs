[package]
name = "dsh-persistence"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "M0 契约基建 (dsh-persistence:seam)：SessionPersistence trait + SessionInspection/SessionPreparation/SessionLocation/Revision + 错误。"

[dependencies]
dsh-brand = { path = "../dsh-brand" }
dsh-session = { path = "../dsh-session" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
