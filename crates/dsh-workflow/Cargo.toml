[package]
name = "dsh-workflow"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Rust port of the dsh workflow capability seam (meta validation + event skeleton + fatal-code classification; JS execution stays an honest stub)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
