[package]
name = "dsh-session"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "M0 契约基建 (dsh-session:types)：SessionEventMap 语义类型全量 + SessionEvent 信封 + surface + TurnEndReason + KNOWN 词表。"

[dependencies]
dsh-brand = { path = "../dsh-brand" }
dsh-llm = { path = "../dsh-llm" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
