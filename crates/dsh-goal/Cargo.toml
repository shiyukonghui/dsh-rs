[package]
name = "dsh-goal"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Rust port of the dsh goal capability seam (CAS state machine + event-sourced replay fold + projection + round-driver predicate)."

[dependencies]
dsh-session = { path = "../dsh-session" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
